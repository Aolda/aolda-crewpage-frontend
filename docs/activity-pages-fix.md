# Activity 목록·상세 접속 오류 수정

## 원인

- 운영 프로젝트 목록은 UUID ID와 `background`를 반환하며, 선택적인 `backgroundImage`는 반환하지 않는다. 현재 22건의 배경 URL이 비어 있어 `backgroundImage.url` 직접 접근이 목록 렌더링을 중단시켰다.
- 상세 라우트가 UUID를 `Number()`로 변환한 뒤 truthy 여부를 검사하여, 요청 없이 로딩 화면에 남았다.
- 상세 라우트에 `USE_MOCK = true`가 남아 있었고 상세 템플릿도 같은 이미지 접근 오류를 가지고 있었다.

## 변경

- 프로젝트 ID를 문자열로 통일하고 UUID 형태를 확인한 뒤 실제 상세 API를 호출한다. 숫자 ID용 예시 목록도 UUID 형식으로 변경했다.
- 목록·상세 이미지에 공통 대체 로직을 적용한다. `background.url` → 레거시 `backgroundImage.url` → 기존 로컬 마스코트 순서로 표시한다.
- 상세의 프런트엔드 mock 분기를 제거한다. 약칭이 없는 활동의 제목은 영문명/한글명으로 대체한다.
- 잘못된 주소, 404, 네트워크/서버 오류에 안내와 목록 링크를 표시하고 조회 실패는 재시도할 수 있게 한다. 페이지 이동/재시도 후 도착한 이전 응답은 무시한다.
- 추가 승인 후 기존 빌드 차단 오류도 수정했다. 모집 상태의 정상/레거시 표기를 함께 정의하고, 준비중 상태 및 홈페이지 약칭 타입을 실제 응답에 맞췄다. 예시 데이터에 필수 배경값을 추가하고 존재하지 않는 `radius.xs`는 정의된 `radius.sm`으로 교체했다.
- 홈페이지 반복 배열의 타입이 `any[]`로 유실되지 않게 하며 `npm run typecheck` 명령과 관련 회귀 테스트를 추가했다.
- 백엔드, DB, 운영 환경, 인증 및 기존 배포 전용 호환 패치는 변경하지 않는다.

## 검증 (2026-08-28 KST)

- `npm run test:activity`: 12개 통과. UUID 전달, API 응답/404 전달, 빈 이미지, 레거시 이미지, 카드·상세 렌더링, 오류 안내, 모집 상태 호환성, 홈페이지 약칭/예시 데이터 및 디자인 토큰을 검증한다. 합성 데이터만 사용하며 운영 데이터 쓰기는 없다.
- `npm run test:crew-academic`: 기존 3개 통과.
- 변경된 TS/TSX 파일 대상 ESLint 및 `git diff --check`: 통과.
- 로컬 Next 개발 서버에서 `/api` 프록시를 운영 공개 API로 연결하여 브라우저 검증: 목록 22건, AAS 카드 클릭 → 실제 UUID 상세 표시, 같은 상세 주소 직접 재접속, 잘못된 ID 안내, 미존재 UUID 오류 안내·재시도·목록 복귀.
- `npm run typecheck`: 오류 0건. 초기 main 10건 → 첫 수정 8건 → 추가 수정 후 0건.
- `NEXT_PUBLIC_BASE_URL=/api BACKEND_INTERNAL_URL=https://ahpconsole.ajou.app npm run build`: 컴파일, TypeScript 검사, 정적 페이지 생성, 최적화 전체 통과. 생성된 `.next/required-server-files.json`의 `config.typescript.ignoreBuildErrors`는 `false`다. `ts-ignore`, `ts-nocheck`, 새 타입 검사 우회 설정은 추가하지 않았다.
- 해당 빌드를 `npm start`로 실행하여 운영 공개 API를 읽기 전용으로 연결했다. 목록 22건, 카드 클릭 → UUID 상세 표시, 상세 직접 재접속, 잘못된 주소 안내를 브라우저에서 확인했다. 개발 서버가 아닌 프로덕션 실행 검증이다.
- 기존 운영용 `Dockerfile.deploy` 절차로 Linux/amd64·Node 20 이미지를 빌드했다. 로컬 검증 이미지 `aolda-crewpage-frontend:activity-build-check`에서 빌드 전체 통과 및 Node `v20.20.2`, `ignoreBuildErrors: false`를 확인했다. `/api` 프록시 검증은 운영 공개 API를 읽기만 하며, 이 테스트 이미지를 운영에 배포한 것은 아니다.
- Linux 컨테이너를 768MiB/0.6 CPU로 로컬 실행하여 API 200·활동 22건, 홈페이지/목록/상세 HTTP 200, 브라우저에서 목록 카드 22개·카드 클릭 후 실제 UUID 상세 표시를 확인했다. 배포용 이미지 생성과 기동을 막던 빌드 오류는 해소되었다.

## 별도 후속 검토 사항 (이번 수정 범위 밖)

- 백엔드는 존재하지 않는 UUID에 현재 404가 아닌 500을 반환하므로 운영에서는 일반 조회 실패 문구가 표시된다. 404 문구는 합성 응답 기반 테스트로 검증했다.
- 백엔드 상세 응답 자체가 `PROJECT_DETAIL_EXAMPLE`의 프로젝트 유형·진행회차·갤러리를 일부 사용한다. 프런트엔드 mock 제거는 이 백엔드 예시 값까지 제거하지 않는다.
- 상세 참여자 배열은 백엔드의 관리자 override 기준이며 목록 참여자 집계와 차이가 있을 수 있다. 기존 어두운 테마의 상세 헤더 글자 대비도 별도 검토가 필요하다.
- 기존 lockfile의 의존성 감사 경고는 별도 보안 검토 대상이다. Docker의 런타임 의존성 설치에서 high 6건이 보고되었으며, 이번 빌드 수정에서는 의존성을 업데이트하거나 `npm audit fix`를 실행하지 않았다.

## 운영 반영 조건

추가 변경을 포함한 PR의 사용자 승인 및 main 병합 확인 전에는 운영 반영하지 않는다. 이후에도 실제 병합 diff와 운영 SHA를 비교한다. 기존 운영의 호환 패치는 먼저 백업하되, 이번 브랜치의 타입·Badge 수정과 중복 적용하거나 `ignoreBuildErrors`를 다시 추가하지 않는다. `/api` 빌드 환경값과 내부 백엔드 연결 설정은 보존한다. 이 수정에는 백엔드 배포나 DB 작업이 필요 없다.
